{% block collection_widget -%}
	{#
	<div class="form-group" style="margin-bottom: 0"><h3 style="margin-bottom: 0">{{ form_label(form) }}</h3></div> #}
		{% for children in form %}
			{% for child in children %}
				{{ form_row(child) }}
			{% endfor %}

			<div class="form-group" style="margin-bottom: 0">
				<div class="col-sm-2"></div>
				<div class="col-sm-10">
					<ul class="nav nav-pills nav-horizontal">
						<li><a href="#"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Add</a></li>
						<li><a href="#"><span class="glyphicon glyphicon-remove" aria-hidden="true"></span> Remove</a></li>
					</ul>
				</div>
			</div>
			<hr style="margin-top: 15px">
		{% endfor %}
{% endblock collection_widget %}